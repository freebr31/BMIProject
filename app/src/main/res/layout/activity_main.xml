<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_margin="20dp"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">



    <EditText
        android:id="@+id/edittext_weight"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Type your Weight in Kilograms"
        android:inputType="numberDecimal"
        android:maxLength="6" />

    <EditText
        android:id="@+id/edittext_height"
        android:inputType="numberDecimal"
        android:maxLength="6"
        android:layout_below="@+id/edittext_weight"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:hint="Type your Height in cm"

        />

    <Button
        android:id="@+id/butnCalculate"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Calculate"
        />

    <ImageView
        android:id="@+id/imageView4"
        android:layout_centerHorizontal="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:rotationX="-4"
        android:visibility="visible"
        app:srcCompat="@drawable/we"
        android:layout_alignTop="@id/edittext_height"/>


</RelativeLayout>